The smallest decision signal — a colored dot. Green = approve, red = reject; `working` (amber) + `pulse` for an in-progress state. Pair with a label inline.

```jsx
<DecisionDot decision="approve" /> Approved
<DecisionDot decision="working" pulse /> Working…
```
