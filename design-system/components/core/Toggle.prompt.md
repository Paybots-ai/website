Compact on/off switch; track fills brand blue when on. Controlled — pass `checked` and `onChange(next)`.

```jsx
<Toggle checked={flag} onChange={setFlag} label="Show evidence panel" />
<Toggle size="sm" checked={flag} onChange={setFlag} />
```

Sizes: `sm` / `md`. Focus shows the system 2px ring.
