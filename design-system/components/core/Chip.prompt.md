Pill-shaped filter chip for tab-like filter rows (e.g. All / Approved / Rejected). Toggles a soft-blue active state.

```jsx
<Chip active>All</Chip>
<Chip onClick={...}>Approved</Chip>
<Chip size="sm">Compact</Chip>
```

Sizes: `sm` / `md`. Focus shows the system 2px ring.
